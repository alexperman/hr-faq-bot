# render.yaml — AlterZahen deployment
# Auto-generated by Hermes
services:
  - type: web
    name: hr-faq-bot
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --access-logfile -
    directory: backend
